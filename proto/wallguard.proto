syntax = "proto3";

package wallguard;

service WallGuard {
  // Sample
  rpc Sample (SampleMessage) returns (SampleResponse);
}

// Input -----------------------------------------------------------------------------------------------------------------

message SampleMessage {
  uint32 value = 1;
}

// Output -----------------------------------------------------------------------------------------------------------------

message SampleResponse {
  uint32 value = 1;
}
