syntax = "proto3";

package wallguard_service;

import "commands.proto";

message ControlChannelRequest {
    string app_id = 1;
    string app_secret = 2;
}

service WallGuard {
  rpc ControlChannel(ControlChannelRequest) returns (stream wallguard_commands.WallGuardCommand);
}
